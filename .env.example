# Server Configuration
PORT=3001
NODE_ENV=development

# Better Auth Configuration
BETTER_AUTH_SECRET=your-secret-key-here
BETTER_AUTH_URL=http://localhost:3001  # Will be https://eatzy-auth.intern.eatzy.com in staging

# API Service Configuration
API_SERVICE_URL=http://localhost:8080
API_SERVICE_TOKEN=your-api-service-shared-secret  # Shared secret for auth-service ↔ API communication

# Database Configuration (same credentials as kudoz-backend, different DB name)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=auth_service
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/auth_service