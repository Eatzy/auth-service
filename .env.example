# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (REQUIRED - Cannot be stored in DB)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=auth_service

# SSL Configuration for Production
DB_SSL=false
DB_CA_CERT=
DB_SSL_REJECT_UNAUTHORIZED=true

# Core Auth Secrets (REQUIRED - Must be in env for security)
BETTER_AUTH_SECRET=your-super-secret-key-for-jwt-signing-change-this-in-production
BETTER_AUTH_URL=http://localhost:3001

# Note: Other configurations (API_SERVICE_URL, TRUSTED_ORIGINS, Social Auth, etc.)
# are now managed via database migrations and can be updated through:

